import React from 'react';
import Button from '@mui/material/Button';

function ContactUsBar() {
    return (
        <div>
            <Button variant='contained' sx={{ backgroundColor: 'rgb(206, 141, 20)'}}>contact us</Button>
        </div>
    );
};

export default ContactUsBar;