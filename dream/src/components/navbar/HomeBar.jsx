import React from 'react';
import Button from '@mui/material/Button';

const HomeBar = () => {
    return (
        <div >
            <Button variant='contained' sx={{ backgroundColor: 'rgb(206, 141, 20)'}}>Home</Button>
        </div>
    );
};

export default HomeBar;
